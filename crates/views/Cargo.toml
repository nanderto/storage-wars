[package]
name = "views"
version = "0.1.0"
edition = "2021"

[dependencies]
models = { path = "../models" }
tree_ops = { path = "../tree_ops" }
database = { path = "../database" }
scanner = { path = "../scanner" }
utils = { path = "../utils" }
gpui = "0.1"