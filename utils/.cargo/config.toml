# Build configuration for the utils component.

[build]
# Use all available logical CPUs for parallel compilation.
jobs = 0

[profile.dev]
# Faster incremental builds during development.
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = "thin"