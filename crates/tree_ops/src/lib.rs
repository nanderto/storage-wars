/// Flatten a nested `FsNode` tree into a `Vec<UiNode>` respecting expanded
/// paths, with `scan_progress` expressed as a fraction of the largest sibling.
pub mod flatten {
    // Placeholder – implementation will follow.
}

/// Rebuild an `FsNode` hierarchy from a flat `Vec<DbNode>` list using
/// `parent_id` references.
pub mod rebuild {
    // Placeholder – implementation will follow.
}