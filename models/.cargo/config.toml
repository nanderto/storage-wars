[build]
# Emit compiler warnings as errors in CI; remove this line for local development
# rustflags = ["-D", "warnings"]

[profile.dev]
# Faster incremental builds during development
incremental = true