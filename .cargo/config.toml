[build]
# Use the faster linker on Linux if available; harmless on macOS/Windows.
# Uncomment and adjust for your platform as needed.
# rustflags = ["-C", "link-arg=-fuse-ld=lld"]

[alias]
run-app = "run --bin storage-wars"