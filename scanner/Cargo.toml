[package]
name = "scanner"
version = "0.1.0"
edition = "2021"
description = "Multi-threaded filesystem scanner using std::thread"
authors = []

[[bin]]
name = "scanner"
path = "src/main.rs"

[lib]
name = "scanner"
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[profile.dev]
opt-level = 0
debug = true